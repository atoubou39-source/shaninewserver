import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hakkal.hakkalcompany',
  appName: 'Hakkal Trading',
  webDir: 'dist'
};

export default config;
